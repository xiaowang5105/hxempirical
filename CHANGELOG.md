# 1.6.3

- **失败模型记录**：保留 `capture` 包装内部的实际返回码，失败回归不会复用上一次估计的 N、R² 或模型快照。
- **打包路径保护**：只迁移支持的命令文件参数；保留 generate、标签、注释等普通字符串。缺失输入、文件参数含宏、cd 和多文件 append 会停止打包并说明原因。
- **迁移后继续工作**：ZIP 中的项目文件、历史命令和文件设置一起改为相对路径；在新目录打开、再次导出和再次打包均使用迁移后的文件。修复默认随机数生成器导致的项目恢复失败。
- **后台预览**：Stata 文件读取和预览数据提取移到后台，使用临时 frame；取消后丢弃结果，等待当前读取结束并清理后恢复操作。

# 1.6.2

## 2026-09-07 数据检查与研究结果交付

- 全文件流式编码识别，修复 UTF-8 字符被 64 KB 截断时误判编码的问题。
- CSV 解析完整逻辑记录，检查后部前导零、长整数、引号与列数；后台扫描避免在界面线程执行全文件预检。
- 模型比较和共同样本导出支持子集选择；增加模型命名、TSV 表格和更多估计量／前缀识别。
- 普通 merge 执行前检查两表键类型、缺失和唯一性，失败保留原数据与估计结果。
- 项目 ZIP 含项目资源、识别出的本地数据依赖、相对路径脚本、SHA-256 文件清单和项目创建时的 Stata 环境。
- 恢复点频率可设为每步、每 5／10 步或仅手动；默认维持每步完整保存，降低频率时明确提示恢复范围。
- 增加 CSV 边界、模型识别、表格内容、迁移重放和真实 Stata 合并故障测试。

# 1.6.1

## 2026-09-07 数据转换与载入保护

- 转换写入同目录临时 DTA，保存成功后再发布；已存在目标要求确认覆盖并支持原子替换，失败保留原文件。目标在转换期间出现或检测到变化时停止替换。
- 批量转换在执行前检查重复目标（包括跨格式、大小写同名），展示已有 DTA 覆盖清单；保留默认跳过策略。
- 输出必须为 DTA，拒绝与原文件相同的路径或硬链接目标；失败转换的重放命令保留为注释。
- 转换后的载入统一确认、后台执行并检查返回码。新增事务式数据载入，失败恢复数据与结果，成功清除旧估计状态。
- 项目记录转换的实际导入、保存和 frame 清理步骤，单独记录载入；修复多行失败步骤的脚本导出。导出重放会覆盖转换输出，需核对原文件与输出路径。
- 拆出独立文件操作模块，新增载入故障、取消操作、转换重放及文件覆盖保护测试。

# 1.6.0

## 2026-09-06 项目恢复保护

- 面板向导和通用面板页的 `xtset` 进入项目日志，保持声明与估计的重放顺序。
- IV 快照追加内生变量、工具变量、估计方式等字段，保留旧 33/34 字段记录兼容。
- 表格编辑器和公式栏读取原始值；未修改提交跳过写入，避免显示精度、标签或日期格式改变数据；字符串保留首尾空白。
- 修复嵌套 Stata 命令重复转义普通引号的问题；运行期间暂停命令预览刷新并拒绝再次执行面板准备步骤。

- 项目载入失败恢复原数据、估计结果和随机数状态；多 frame 会话明确拒绝创建或恢复不完整快照。
- 每个完成步骤生成含数据的恢复点；手动保存保留上一版项目。检查模型资源、10,000 步和 64 MB 上限。
- 资源真实路径检查覆盖新文件的祖先目录；拒绝符号链接和 junction。写入同步后原子替换，不支持时保留旧文件。
- 清理本次失败写入及不再被项目、备份、恢复点引用的旧数据快照。
- 监控行预览限制前 5,000 行，显示抽样范围；全量样本统计保持原行为。
- 同步包日期、帮助正文、README 和项目说明。完整多 frame 及环境依赖封装仍未实现。

- 修复监控和界面内部查询覆盖 Stata `r()` 返回结果的问题，保留标量、宏和矩阵。
- 变量表达式仅在正式运行时求值；监控保留随机数和排序随机数状态。
- 最近模型按完整设置区分，包括样本条件、固定效应、标准误、权重及完整命令；兼容已有最近记录。
- 新增研究项目：数据快照、运行日志、设置恢复、do-file 导出、模型比较和共同样本重估脚本。
- 拆分项目生命周期、项目文件格式、模型设置和运行结果模块。
- Java 构建校验覆盖全部源文件及 JAR 的 SHA-256；新增项目行为测试和真实 Stata 发布验收入口。
