# 1.6.3 验证记录

环境：2026-09-07，Windows / StataNow 19.5 MP，真实 SFI，Java 11 字节码。

## 修复验证

- 真实 Stata：成功回归后运行 `capture regress` 的失败案例，返回 111，N 为缺失，模型数量不增加；成功 capture 仍捕获 N=40。
- Java：ZIP 保留普通文件名字符串；支持的文件参数和界面文件设置使用相对路径。项目移动、删除原 CSV 后可再次打包；宏、cd 和多文件 append 被拒绝。
- 真实 Stata：解压项目到新目录，恢复数据和随机数状态，再次导出运行，核对观测数和字符串代码列。新快照保存实际随机数生成器，兼容旧 default 记录。
- 真实 Stata：后台预览读取前 200 行，保留原数据的 40 条观测；在 Swing 界面线程调用读取器会被拒绝。

## 最终发布检查

最终全部 14 项真实 Stata 测试通过，包括安装完整性、生命周期、路径遮挡、merge 预检和工作台流程回归。日志位于工作区 `hx-fix-validation/research-163-final`。

3 组 Java 行为测试与 6 项仓库／发布检查全部通过，使用最终发布 JAR。源码清单包含 9 个 Java 模块。

全套回归曾发现普通 merge 成功后遗留内部 `_rc` 导致误报；现仅对显式 capture 读取内部返回码，普通 merge 与失败 capture 均通过重跑验证。

- JAR SHA-256：`7cb15b87cf3b181442c0d06ea667121a0c2491cf7ba94b669be6db2b92550c85`。
- ZIP SHA-256：`895aa9f76f3e8d7e0ff64cc3bd5f7813c43dedd68803552325aa0798c728e39d`。
- 发布包：41 个受管文件，46 个 ZIP 条目，18 个 Base64 分片。

## 边界

尚未完成完整鼠标旅程、所有新增估计量、Stata 17/18、macOS 和大样本性能验收。后台读取及临时 frame 已测试，取消按钮和关闭窗口的完整交互仍需人工验收；取消会等待当前 Stata 读取和清理完成。

文件迁移覆盖明确列出的命令文件参数；宏、目录切换、多文件 append 和缺失输入会停止打包。自定义程序、续行语法、其他文件命令、网络资源及第三方命令版本需人工核对。CSV 单条记录上限 16 MB，编码可手动指定。

模型表格仍为 TSV；完整 Word／LaTeX 表图交付、更多模型的共同样本重估、快照复用和后台保存尚未完成。
