# 1.6.2 验证记录

环境：2026-09-07，Windows / StataNow 19.5 MP，真实 SFI，Java 11 字节码。

## 已完成的定向验证

- Java：UTF-8 64 KB 字符边界、文件后部 GB18030、后部前导零与长整数、多行字段、BOM、引号内分隔符、异常引号与列数。
- Java：模型前缀识别、模型名称持久化、选定模型 TSV 内容、ZIP 相对路径及文件清单；既有项目与文件保护测试通过。
- 真实 Stata：生产导入命令处理 311 条含后部前导零和多行字段的记录；保持 `001234`。
- 真实 Stata：项目 ZIP 解压到不同目录后运行 replay.do，核对最终观测和代码列；bootstrap 与 svy 结果捕获和模型快照成功。
- 真实 Stata：merge 重复键及缺失键阻止合并，保留原数据和估计系数；合法合并成功，临时 frame 清理。
- 工作台集成测试增加每 5 步数据恢复点验证；默认仍每步保存。

## 最终发布检查

最终全部 14 项真实 Stata 测试通过，含安装完整性、生命周期、路径遮挡和扩展后的工作台回归。运行日志位于工作区 `hx-fix-validation/research-162-full`。
6 项仓库／发布检查及 3 组 Java 行为测试全部通过，覆盖最终源码与 JAR。源码清单包含 8 个 Java 模块。

- JAR SHA-256：`795beb7dbd598154f4b81eaa5140d24c5208dbc9f3b7d395e180801ec7006f8c`。
- ZIP SHA-256：`6534e75be124d3625d1506a35d6916129a0d1ec5619f7578d37e7497aae90328`。
- 发布包：41 个受管文件，46 个 ZIP 条目，18 个 Base64 分片。

## 边界

尚未完成完整鼠标旅程、所有新增估计量、Stata 17/18、macOS 和大样本性能验收。CSV 单条记录上限 16 MB，编码自动识别仍存在不同字符集之间的固有歧义，可手动指定编码。

模型表格为 TSV，尚无完整 Word／LaTeX 表图排版。共同样本重估仍限普通线性模型。恢复点频率降低会增加异常退出时未保存的步骤；单次快照仍完整写入 DTA，后台保存与快照复用待完成。

ZIP 自动复制引号内可识别的本地数据文件；含宏、未加引号、动态目录、网络资源和第三方命令版本需人工核对。环境信息记录于项目创建时，旧项目可能缺失。标准 do-file 导出继续使用绝对路径，ZIP 内 replay.do 提供相对路径重放。
