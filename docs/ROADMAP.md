# 核心研究流程路线图

目标：用户从原始数据完成清洗、面板声明、基准与 IV 回归、模型比较、表图导出；保存后可以继续工作，并在新的 Stata 会话重现主要结果。

第一阶段优先覆盖单 frame、常见数据文件、OLS／固定效应／IV／基础 DID。其他命令逐类验证配置、执行、模型保存、比较和重放能力。

## 数据保护与导入

- [x] 项目读取失败回滚；完成步骤恢复点与手动备份（1.6.0）。
- [x] 表格和公式栏编辑使用原始值（1.6.0）。
- [x] 转换先保存临时文件，确认后发布目标；转换失败保护旧文件（1.6.1）。
- [x] 批量重复目标检查、覆盖清单确认及默认跳过（1.6.1）。
- [x] 转换后载入统一确认、失败回滚、载入步骤独立记录（1.6.1）。
- [x] CSV 多行字段、完整文件前导零／长整数和混合类型检查（1.6.2）。
- [x] 普通 merge 的键类型、缺失和唯一性预检，保留 Stata 匹配报告（1.6.2）。
- [x] 原生文件预览后台读取、取消后丢弃结果并清理临时 frame（1.6.3）。
- [ ] 统一数据版本与旧结果标记。
- [ ] 导入预检和项目保存的大数据性能与取消支持。

## 模型与结果交付

- [x] 面板声明进入项目日志；新 IV 设置往返（1.6.0）。
- [ ] 前缀与不同估计量的模型捕获，逐类标注支持边界。
- [x] 模型命名、子集选择和所选模型 TSV 比较表（1.6.2）。
- [x] capture 内部失败返回码与陈旧模型结果保护（1.6.3）。
- [ ] 细化数据版本与样本差异展示。
- [ ] 常用模型的共同样本重估。
- [ ] 回归表、描述统计和研究图形的可编辑导出。

## 复现与验收

- [x] 面板、IV、原值编辑以及转换／载入的真实 Stata 回归用例。
- [x] 项目 ZIP、相对路径重放、可识别本地依赖复制及 Stata 环境记录（1.6.2）。
- [x] 按文件参数迁移项目命令与设置；解压后打开、重放及再次打包验证（1.6.3）。
- [ ] 宏／动态路径解析和第三方命令版本锁定。
- [ ] 完整鼠标操作、运行期间交互、大样本性能及跨平台验收。
- [ ] 新会话中运行完整研究项目，核对系数、实际样本及导出图表。

后续能力：多 frame 及链接封装、更广模型族、完整环境迁移。路线图中的完成项表示相应实现和明确测试范围，整条研究流程仍需最终验收。
